---
/**
 * ═══════════════════════════════════════════════════════════
 * SYSTEM EXECUTION.ASTRO — LYRIX OS FINAL CTA
 * Wrapper for the React SystemExecution component
 * ═══════════════════════════════════════════════════════════
 */

import type { Lang } from '../i18n/ui';
import CTAComponent from './CTA';

interface Props { lang?: Lang; }
const { lang = 'en' } = Astro.props;
---

<section id="execute">
  <CTAComponent lang={lang} client:visible />
</section>