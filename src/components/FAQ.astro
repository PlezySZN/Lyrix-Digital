---
/**
 * ═══════════════════════════════════════════════════════════
 * KNOWLEDGE BASE.ASTRO — LYRIX OS FAQ MANUAL
 * Wrapper for the React KnowledgeBase component
 * ═══════════════════════════════════════════════════════════
 */

import type { Lang } from '../i18n/ui';
import FAQComponent from './FAQ';

interface Props { lang?: Lang; }
const { lang = 'en' } = Astro.props;
---

<FAQComponent lang={lang} client:visible />