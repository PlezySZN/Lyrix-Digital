---
/**
 * ═══════════════════════════════════════════════════════════
 * PORTFOLIO.ASTRO — LYRIX OS PORTFOLIO
 * Wrapper for the React Portfolio component
 * ═══════════════════════════════════════════════════════════
 */

import type { Lang } from '../i18n/ui';
import PortfolioComponent from './Portfolio';

interface Props { lang?: Lang; }
const { lang = 'en' } = Astro.props;
---

<div class="cwv-defer">
<PortfolioComponent lang={lang} client:visible />
</div>
