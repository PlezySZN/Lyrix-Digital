---
/**
 * ═══════════════════════════════════════════════════════════
 * PROJECT LOGS.ASTRO — LYRIX OS PORTFOLIO
 * Wrapper for the React ProjectLogs component
 * ═══════════════════════════════════════════════════════════
 */

import type { Lang } from '../i18n/ui';
import ProjectLogsComponent from './ProjectLogs';

interface Props { lang?: Lang; }
const { lang = 'es' } = Astro.props;
---

<ProjectLogsComponent lang={lang} client:visible />
