---
/**
 * ═══════════════════════════════════════════════════════════
 * CLIENT TELEMETRY.ASTRO — LYRIX OS TESTIMONIALS
 * Wrapper for the React ClientTelemetry component
 * ═══════════════════════════════════════════════════════════
 */

import type { Lang } from '../i18n/ui';
import ClientTelemetryComponent from './ClientTelemetry';

interface Props { lang?: Lang; }
const { lang = 'es' } = Astro.props;
---

<ClientTelemetryComponent lang={lang} client:visible />