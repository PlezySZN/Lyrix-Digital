---
/**
 * ═══════════════════════════════════════════════════════════
 * DEPLOYMENT SEQUENCE.ASTRO — LYRIX OS PROCESS
 * Wrapper for the React DeploymentSequence component
 * ═══════════════════════════════════════════════════════════
 */

import type { Lang } from '../i18n/ui';
import DeploymentSequenceComponent from './DeploymentSequence';

interface Props { lang?: Lang; }
const { lang = 'es' } = Astro.props;
---

<DeploymentSequenceComponent lang={lang} client:visible />
