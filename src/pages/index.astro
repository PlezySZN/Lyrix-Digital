---
/**
 * ═══════════════════════════════════════════════════════════
 * ROOT INDEX — LYRIX OS DEFAULT LOCALE
 * Serves the English landing page without a redirect to
 * improve performance and avoid extra hops.
 * ═══════════════════════════════════════════════════════════
 */

import type { Lang } from '../i18n/ui';
import Layout from '../layouts/main.astro';
import Hero from '../components/Hero.astro';
import Services from '../components/sections/Services.astro';
import Portfolio from '../components/sections/Portfolio.astro';
import Process from '../components/sections/Process.astro';
import Reviews from '../components/sections/Reviews.astro';
import FAQ from '../components/sections/FAQ.astro';
import CTA from '../components/sections/CTA.astro';

const lang: Lang = 'en';
const keywords = 'web design puerto rico, web development PR, digital agency puerto rico, website creation, web design san juan, drone video production PR, SEO puerto rico, digital marketing PR';
---

<Layout lang={lang} keywords={keywords} canonicalPath="/en/">
	<Hero lang={lang} />

	<main id="main-content">
		<Portfolio lang={lang} />
		<Services lang={lang} />
		<Reviews lang={lang} />
		<Process lang={lang} />
		<FAQ lang={lang} />
		<CTA lang={lang} />
	</main>
</Layout>

